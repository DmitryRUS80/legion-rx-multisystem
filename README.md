## Legion RX 4.0.16 Alpha — Track Day PIT

# Базовая линия: 4.0.10 Alpha — Warmup Voice

Основа: 4.0.8 Announcer Start.

## Что добавлено
- Настраиваемый предстартовый прогрев: 1–5 минут.
- Выбранные минуты — полное время от нажатия `СТАРТ / ПРОГРЕВ` до HORN. Финальный отсчёт 1–10 секунд входит в это время и не прибавляется сверху.
- В начале прогрева LapWiz запускается в `Free Practice` и остаётся в этой аппаратной сессии через HORN и начало гонки.
- В Free Practice поддержан второй этап штатного протокола SkyRC: после первого IR-прохода отправляется `START_TIMING(0)` / command `0x06`.
- Во время прогрева каждый валидный проход зарегистрированного транспондера даёт пользовательский Bleep.
- На пульте показывается `обнаружено X / N`; у пилота появляется `НА ТРАССЕ ✓`. Повторные проходы не увеличивают X / N.
- После HORN предстартовый счётчик скрывается, гоночный таймер стартует с нуля.
- Первый проход пилота после HORN: Bleep + `СТАРТ ✓`, круг не добавляется.
- Следующий проход становится кругом 1. Его время считается от HORN, поэтому первый круг может быть длиннее последующих, если засечка находится после стартовой решётки.
- Последующие круги используют аппаратные интервалы LapWiz (`workTicks`, 10 мс на тик) с резервом по таймеру приложения.

## Предстартовый диктор
В 4.0.10 минутные отметки 5/4/3/2/1 и фраза «30 секунд» переведены на записанные WAV пользователя. Отдельной записанной фразы приглашения на трассу в voice-pack пока нет, поэтому только эта короткая часть временно остаётся через SpeechSynthesis.

Для полной замены системного голоса на штатный голос Legion RX нужны файлы из `VOICE_WARMUP_TODO.txt`.

## Важно для полевого теста
Главная проверка 4.0.10 — убедиться на реальном LapWiz, что одна сессия Free Practice стабильно переживает прогрев → HORN → весь заезд без STOP/START на старте.

Web Bluetooth: Chrome на Android/Windows + HTTPS (например GitHub Pages).


### 4.0.15 additions
- Track Day / Free Practice 10–120 min with separate reports.
- Per-pilot lap history: best / average / worst and every lap time.
- Cockpit race management: skip heats, shorten qualifying, finish competition early.
- Built directly on the working 4.0.14 mobile-audio base.


### 4.0.16 additions
- Track Day PIT timeout: 1:00 / 1:30 / 2:00 / 3:00 / Off.
- Automatic PIT after inactivity; incomplete lap is discarded.
- First pass after PIT = PIT OUT / new timing reference; next pass = normal lap.
- PIT count and pit time are stored in Track Day statistics.
- Built directly on 4.0.15 without changes to RallyCross, LapWiz Core, or audio architecture.

## 4.0.17 Alpha
В RallyCross на пульте теперь есть отдельные быстрые кнопки «Пропустить» и «Отмена / завершение». Первая отменяет только текущий заезд после подтверждения, вторая открывает расширенное управление программой соревнования.

## 4.0.18 TEST — Layout Lab
Эта папка является отдельной экспериментальной веткой интерфейса. Правая колонка виджетов RallyCross убрана; подробные состояния открываются по нажатию на верхние индикаторы. Кнопка Layout Lab позволяет самостоятельно перемещать и изменять размеры основных блоков пульта. Макет сохраняется локально в браузере и может быть скопирован как JSON.


## 4.0.19 TEST — Independent Control Buttons
- Нижние кнопки RallyCross стали независимыми элементами Layout Lab.
- Каждую нижнюю кнопку можно отдельно перемещать и изменять её размер.
- Любую нижнюю кнопку можно скрыть крестиком в Layout Lab; восстановление — через кнопку «Кнопки» в панели редактора.
- Добавлена отдельная нижняя кнопка «Пропустить заезд».
- Скрытие и расположение сохраняются локально как часть схемы Layout Lab v2.
- Спортивная логика, Track Day, PIT, LapWiz и аудио не изменялись.


## 4.0.20 TEST — Track Day Mobile Layout Fix
- Track Day получил отдельную responsive-сетку.
- Landscape: пилоты слева, таймер/пульт справа.
- Portrait: видны лимит, прошло, лучший круг и осталось; служебные LapWiz/«Как считаем» свернуты и расположены после списка пилотов.
- Спортивная логика, PIT, аудио и RallyCross не изменялись.


## 4.0.21 TEST — Mobile Control Fix
Compact theme-aware header controls, portrait Track Day main timer/ring, and 1–60 s Track Day minimum-lap filter.


## 4.0.22 TEST — Pilot Cards & Flags
- Страна пилота хранится в общей базе и показывается флагом.
- Клуб `LegionRX` получает фирменную подпись LEGION RX.
- Строки пилотов в RallyCross и Track Day увеличены и остаются адаптивными.
- Исторические профили без страны продолжают работать без миграции.

## 4.0.23 TEST — Offline Audio Ready
- При первом запуске версии Legion RX собирает и проверяет полный offline-пакет из 27 обязательных ресурсов.
- После проверки показывается `OFFLINE READY ✓`; на следующих запусках идёт только быстрая проверка локального кэша.
- Весь текущий записанный voice-pack, HORN, Bleep и silence хранятся локально и используются cache-first.
- Убрана подмена отсутствующего WAV/MP3 страницей `index.html` в Service Worker.
- Системная SpeechSynthesis-фраза приглашения на трассу временно отключена: предстартовый сценарий больше не зависит от сетевого/системного TTS. Она вернётся только отдельной проверенной записью Oksana.
- Генерация голосов пилотов и новые динамические объявления в эту сборку не входят.

## 4.0.24 TEST — Audio Offline Hotfix
Исправляет Android Chrome `NotSupportedError` при `OFFLINE READY 27/27`: бинарные ресурсы теперь валидируются по сигнатуре, аудио проигрывается из локальных Blob URL, а service worker поддерживает byte-range ответы для media.

## 4.0.25 TEST — RallyCross Cockpit
Главный горизонтальный пульт RallyCross переведён на новый approved layout: крупные строки пилотов, BEST/AVG/LAPS, большой timer/ring, компактное верхнее управление и скрытые quick panels. Dark/Light поддерживаются одной адаптивной схемой. Track Day и LapWiz/audio core не менялись.


## 4.0.26 TEST — RallyCross Cockpit Fit

Точечная подгонка правой колонки RallyCross под реальную высоту Android landscape: основные кнопки и таймер сохраняют размер, вторичные раскрываемые строки уменьшены по высоте.

## 4.0.27 TEST — Adaptive Cockpit
RallyCross cockpit получил универсальную адаптацию для phone portrait, tablet/desktop landscape и больших Full HD/2K экранов. Критические элементы остаются на месте, список пилотов скроллится независимо, а размеры интерфейса fluid-scale через CSS clamp()/viewport breakpoints. Нижнее дублирование LapWiz/диктора/результатов убрано; оставлена только строка следующего заезда.

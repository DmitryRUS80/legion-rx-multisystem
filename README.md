# RC64 CURRENT BUILD

Current candidate: **LEGION RX 4.2.0 CLEAN FULL APP RC64 · CLASSIC STATUS CONTROL FIX**.

RC64 is a surgical Classic RC UI/control repair on top of RC63. The neutral Competition Scheduler, EFRA rules, RallyCross, LapWiz and protected foundation are unchanged.

Changes: compact live race-state strip above the Classic RC race timer; whole-second next-event countdown; explicit cockpit grid rows so the status strip can never consume the timer/control area; the strip itself opens Schedule; overlay hit areas remain present only while Schedule is open.

Use **FULL** as the only source for future development. Use **UPLOAD_TO_GITHUB** only as the exact RC63 → RC64 deployment delta.
